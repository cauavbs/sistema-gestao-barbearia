# Copie este arquivo para ".env" e preencha com seus dados reais.
# NUNCA suba o arquivo ".env" real para o GitHub (adicione ele no .gitignore).

PORT=3000

# URL do Realtime Database (a mesma do firebaseConfig atual)
FIREBASE_DATABASE_URL=https://barbearia-sistema-6df17-default-rtdb.firebaseio.com
